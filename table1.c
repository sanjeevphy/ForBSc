#include <stdio.h>
int main(){
  int i, n;
  printf("Give an integer: ");
  scanf( "%d", &n );
  for( i == 1; i <= 10; i++ ) {
    printf( "%d\n", i * n );
  }
  printf( "Table of %d is written.\n", n );
  printf( "Value of i is %d and %d\n", i, i == 1 );
}
